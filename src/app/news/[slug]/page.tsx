export default async function Page() {
//  const res = await fetch(`https://example.com/api/articles/${params.slug}`, {
//    next: { revalidate: 60 }, // ISR caching
//  });

//  if (!res.ok) return <h1>Article not found</h1>;
  return <section></section>;
}
